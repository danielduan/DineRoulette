<phone:PhoneApplicationPage
    x:Class="DineRoulette.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:phone="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone"
    xmlns:shell="clr-namespace:Microsoft.Phone.Shell;assembly=Microsoft.Phone"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    FontFamily="{StaticResource PhoneFontFamilyNormal}"
    FontSize="{StaticResource PhoneFontSizeNormal}"
    Foreground="{StaticResource PhoneForegroundBrush}"
    SupportedOrientations="Portrait" Orientation="Portrait"
    shell:SystemTray.IsVisible="True">

    <!--LayoutRoot is the root grid where all page content is placed-->
    <Grid x:Name="LayoutRoot" Background="#FFBDC3C7">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- LOCALIZATION NOTE:
            To localize the displayed strings copy their values to appropriately named
            keys in the app's neutral language resource file (AppResources.resx) then
            replace the hard-coded text value between the attributes' quotation marks
            with the binding clause whose path points to that string name.

            For example:

                Text="{Binding Path=LocalizedResources.ApplicationTitle, Source={StaticResource LocalizedStrings}}"

            This binding points to the template's string resource named "ApplicationTitle".

            Adding supported languages in the Project Properties tab will create a
            new resx file per language that can carry the translated values of your
            UI strings. The binding in these examples will cause the value of the
            attributes to be drawn from the .resx file that matches the
            CurrentUICulture of the app at run time.
         -->

        <!--TitlePanel contains the name of the application and page title-->
        <StackPanel x:Name="TitlePanel" Grid.Row="0" Background="#FF2980B9" >
            <TextBlock Text="DineRoulette" Margin="0,20,0,28" Style="{StaticResource PhoneTextTitle2Style}" TextAlignment="Center" FontSize="48"/>
        </StackPanel>

        <!--ContentPanel - place additional content here-->
        <Grid x:Name="ContentPanel" Grid.Row="1" Margin="0,0,0,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            
            <Image x:Name="RestaurantImage" Grid.Row="0" Source="/Assets/background.png" HorizontalAlignment="Center" VerticalAlignment="Center"/>
            <Grid x:Name="InfoGrid" Grid.Row="1" Background="#FFecf0f1" Visibility="Collapsed">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <TextBlock x:Name="NameTextBlock" Text="Name" Grid.Row="0" Foreground="Black" Margin="10" TextAlignment="Center" FontSize="24" FontWeight="Bold"/>
                <TextBlock x:Name="PhoneTextBlock" Text="Phone" Grid.Row="1" Foreground="Black" Margin="10" TextAlignment="Center"/>
                <TextBlock x:Name="AddressTextBlock" Text="Address" Grid.Row="2" Foreground="Black" Margin="10" TextAlignment="Center"/>
                <TextBlock x:Name="TaglineTextBlock" Text="Tagline" Grid.Row="3" Foreground="Black" Margin="10" TextAlignment="Center" TextWrapping="Wrap" FontSize="15" FontStyle="Italic"/>
            </Grid>
        </Grid>

        <Grid x:Name="ModalGrid" Grid.Row="1" Background="Black" Canvas.ZIndex="1" Opacity="0.5" Visibility="Collapsed" />
        <ProgressBar x:Name="progressPage" Grid.Row="1" Background="#7FFFFFFF" Foreground="White" Canvas.ZIndex="10" IsIndeterminate="True" Visibility="Collapsed" />


        <Grid Grid.Row="2" Background="#FF2980B9" Height="100" >
            <Button x:Name="FindButton" Grid.Column="0" Content="Find me a nearby restaurant!" Click="FindButton_Click" />
        </Grid>

        <!--Uncomment to see an alignment grid to help ensure your controls are
            aligned on common boundaries.  The image has a top margin of -32px to
            account for the System Tray. Set this to 0 (or remove the margin altogether)
            if the System Tray is hidden.

            Before shipping remove this XAML and the image itself.-->
        <!--<Image Source="/Assets/AlignmentGrid.png" VerticalAlignment="Top" Height="800" Width="480" Margin="0,-32,0,0" Grid.Row="0" Grid.RowSpan="2" IsHitTestVisible="False" />-->
    </Grid>

</phone:PhoneApplicationPage>